diff --git a/node_modules/react-native-mathjax-html-to-svg/index.js b/node_modules/react-native-mathjax-html-to-svg/index.js
index ba615f7..094fec7 100644
--- a/node_modules/react-native-mathjax-html-to-svg/index.js
+++ b/node_modules/react-native-mathjax-html-to-svg/index.js
@@ -74,17 +74,33 @@ const GenerateSvgComponent = ({ item, fontSize, color }) => {
     let svgText = adaptor.innerHTML(item);
 
     const [width, height] = getScale(svgText);
+    const scaledWidth = width * fontSize;
+    const scaledHeight = height * fontSize;
+    const verticalPadding = Math.max(2, fontSize * 0.2);
 
     svgText = svgText.replace(/font-family=\"([^\"]*)\"/gmi, '');
 
-    svgText = applyScale(svgText, [width * fontSize, height * fontSize]);
+    svgText = applyScale(svgText, [scaledWidth, scaledHeight]);
 
     svgText = applyColor(svgText, color);
 
     return (
-        <Text>
-            <SvgFromXml xml={svgText} />
-        </Text>
+        <View
+            style={{
+                width: scaledWidth,
+                height: scaledHeight + (verticalPadding * 2),
+                paddingVertical: verticalPadding,
+                flexShrink: 0,
+                justifyContent: 'center',
+                overflow: 'visible',
+            }}
+        >
+            <SvgFromXml
+                xml={svgText}
+                width={scaledWidth}
+                height={scaledHeight}
+            />
+        </View>
     );
 };
 
